include $HOME/.local/lu-dotfiles/.config/sway/config.d/keywords
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $super+b or $super+v, for horizontal and vertical splits
    # respectively.
    #bindsym $super+b splith
    #bindsym $super+v splitv

    # Switch the current container between different layout styles
    #bindsym $super+s layout stacking
    #bindsym $super+w layout tabbed
    #bindsym $super+e layout toggle split

    # Make the current focus fullscreen
    bindsym $super+f11 fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $super+Shift+f11 floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $super+space+f11 focus mode_toggle

    # Move focus to the parent container
    bindsym $super+a+f11 focus parent